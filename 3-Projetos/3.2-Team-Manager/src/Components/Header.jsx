// hello = () => alert(`Hello!`);

// hello2 = (a, b) => alert(`Hello ${a} ${b}`)

const Header = () => {
    return (
        <header>
            <h1 className="mt-4 p5 bg-primary text-white rounded">Header</h1>
        </header>
    )
}

export default Header